BRICK: BUG-002

STATUS

DISCOVERED

---

ISSUE

SaveTrustedContactAction does not save records to the database.

Users may complete onboarding and receive a success response even though the trusted contact was never persisted.

---

EVIDENCE

Database

trusted_contacts table exists.

TrustedContact model exists.

Current records:

TrustedContact::count() = 2

Controller

Existing save endpoint:

POST /api/v1/auth/trusted-contact

Controller:

AuthController::saveTrustedContact()

Calls:

SaveTrustedContactAction::execute()

Existing Trusted Contact System

Already implemented:

TrustedContactController::store()

Features already present:

- Self-contact validation
- Duplicate validation
- One-contact limit
- Database persistence

Action Evidence

Current action contains:

// TODO: Create trusted_contacts table and save there

Current implementation returns success without creating a TrustedContact record.

---

ROOT CAUSE

Legacy onboarding code was never updated after the trusted_contacts feature was implemented.

The application now has:

- trusted_contacts table
- TrustedContact model
- TrustedContactController

But onboarding still uses an older placeholder action.

---

IMPACT

Risk Level

MEDIUM

User Impact

User may believe a trusted contact was saved.

Trusted contact may never exist in the database.

Emergency workflows may fail because no trusted contact relationship exists.

---

ARCHITECTURE DECISION

Follow LEGO Rule:

REUSE
↓
EXTEND
↓
CREATE

Decision:

- Reuse existing TrustedContact architecture
- Reuse existing validation rules
- Do NOT create a second trusted-contact implementation
- Do NOT create new tables
- Do NOT duplicate business logic

---

FILES INVOLVED

Existing

app/Actions/Auth/SaveTrustedContactAction.php
app/Http/Controllers/Api/V1/AuthController.php
app/Http/Controllers/Api/V1/TrustedContactController.php
app/Models/TrustedContact.php

Database

trusted_contacts

---

NEXT STEP

Inspect:

AuthController
    ↓
SaveTrustedContactAction
    ↓
TrustedContactController::store()

Determine the smallest patch that reuses the existing trusted contact architecture.

No new architecture required.

---

TERMUX COMMANDS USED

cd ~/storage/kin_platform/backend

grep -n "saveTrustedContact" -A 30 -B 10 \
app/Http/Controllers/Api/V1/AuthController.php

grep -n "class SaveTrustedContactAction" -A 120 \
app/Actions/Auth/SaveTrustedContactAction.php

php artisan tinker

App\Models\TrustedContact::count();

App\Models\TrustedContact::latest()->first();

---

STATUS

READY FOR PATCH

BRICK: BUG-002

STATUS: VERIFIED

ROOT CAUSE:
SaveTrustedContactAction returns success without creating a TrustedContact record.

EVIDENCE:

- trusted_contacts table exists
- TrustedContact model exists
- TrustedContactController::store() creates records
- SaveTrustedContactAction contains TODO placeholder
- Frontend onboarding calls /auth/trusted-contact

DECISION:
Patch SaveTrustedContactAction for MVP.

ARCHITECTURE NOTE:
This creates duplicate trusted contact logic.

TECHNICAL DEBT:
TECHDEBT-001
Consolidate trusted contact creation into a shared service after MVP.

RISK:
Low

NEXT ACTION:
Patch SaveTrustedContactAction

ESTIMATED TIME:
5 minutes

EXPECTED RESULT:
NETWORK-001 → 100% COMPLETE
PROJECT → 94%

