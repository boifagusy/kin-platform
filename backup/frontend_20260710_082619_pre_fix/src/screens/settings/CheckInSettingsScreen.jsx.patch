// FIND line ~8 where phone is extracted
const phone = location.state?.phone || localStorage.getItem("kin_phone");

// ADD after it:
const trustedContact = location.state?.trusted_contact;

// FIND the navigate to duress-pin (line ~64)
navigate("/settings/duress-pin", { state: { phone: phone } });

// REPLACE with:
navigate("/settings/duress-pin", { 
  state: { 
    phone: phone,
    trusted_contact: trustedContact 
  } 
});
