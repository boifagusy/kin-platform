// KIN OS — USE USER PREFERENCE HOOK
// Status: Production Foundation
// Purpose: Convenience hook for accessing user preferences

import { useUserPreference } from '../context/UserPreferenceContext';

export { useUserPreference };

// Alternative: re-export with alias
export default useUserPreference;
