import React from 'react'
import { BrowserRouter, Routes, Route } from 'react-router-dom'
import WatchtowerDashboard from './screens/watchtower/WatchtowerDashboard'
import DashboardScreenV2 from './screens/ui-polish/DashboardScreenV2'

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<DashboardScreenV2 />} />
        <Route path="/watchtower" element={<WatchtowerDashboard />} />
      </Routes>
    </BrowserRouter>
  )
}

export default App
